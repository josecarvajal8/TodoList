import React, { ReactNode } from 'react';
import { Home } from './components';

const App: () => ReactNode = () => {
    return (
        <>
            <Home />
        </>
    );
};
export default App;
